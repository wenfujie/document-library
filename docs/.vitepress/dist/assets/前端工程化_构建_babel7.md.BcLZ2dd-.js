import{_ as e,c as o,o as c,a2 as r}from"./chunks/framework.Bek_Mmwg.js";const q=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"前端工程化/构建/babel7.md","filePath":"前端工程化/构建/babel7.md"}'),t={name:"前端工程化/构建/babel7.md"},s=r(`<h2 id="babel7" tabindex="-1">babel7 <a class="header-anchor" href="#babel7" aria-label="Permalink to &quot;babel7&quot;">​</a></h2><p>对 <code>Babel</code> 的配置项的作用不那么了解，是否会影响日常开发呢？老实说，大多情况下没有特别大的影响（毕竟有搜索引擎）。</p><p>不过呢，还是想更进一步了解下，于是最近认真阅读了 <code>Babel</code> 的文档，外加不断编译验证，输出了本篇文章，为了更好的阅读体验，修修改改，最终算是以我个人比较喜欢的方式推进了每个知识点（每一个配置的引入都是有原因的），希望能够帮助你对 <code>Babel</code> 的各种配置有一个更清晰的认识 (已经很懂的小伙伴，无视本文) 。</p><blockquote><p>Babel 是一个 JS 编译器</p></blockquote><p>Babel 是一个工具链，主要用于将 ECMAScript 2015+ 版本的代码转换为向后兼容的 JavaScript 语法，以便能够运行在当前和旧版本的浏览器或其他环境中。</p><p>我们先看看 <code>Babel</code> 能够做什么：</p><ul><li>语法转换</li><li>通过 <code>Polyfill</code> 方式在目标环境中添加缺失的特性(<code>@babel/polyfill模块</code>)</li><li>源码转换(codemods)</li></ul><p>本篇文章的目的是搞明白 <code>Babel</code> 的使用和配置，搞清楚 <code>@babel/runtime</code>，<code>@babel/polyfill</code>，<code>@babel/plugin-transform-runtime</code> 这些作用是什么，插件和预设都是用来干什么的，我们为什么需要配置它们，而不是讲如何进行 <code>AST</code> 转换，如果你对 <code>AST</code> 转换非常感兴趣，欢迎阅读我们的 <a href="https://github.com/areslabs/alita" target="_blank" rel="noreferrer">RN转小程序引擎 Alita</a> 的源码，其中应用了大量的 <code>AST</code> 转换。</p><p>为了更清晰的了解每一步，首先创建一个新项目，例如 <code>babelTemp</code>(你爱取啥名取啥名)，使用 <code>npm init -y</code> 进行初始化，创建 <code>src/index.js</code>，文件内容如下（你也可以随便写点什么）:</p><pre><code>const fn = () =&gt; {    console.log(&#39;a&#39;);};
</code></pre><p>OK，创建好的项目先放在一边，先了解下理论知识:</p><h3 id="核心库-babel-core" tabindex="-1">核心库 @babel/core <a class="header-anchor" href="#核心库-babel-core" aria-label="Permalink to &quot;核心库 @babel/core&quot;">​</a></h3><p>Babel 的核心功能包含在 <code>@babel/core</code> 模块中。看到 <code>core</code> 这个词了吧，意味着<strong>核心</strong>，没有它，在 <code>babel</code> 的世界里注定寸步难行。不安装 <code>@babel/core</code>，无法使用 <code>babel</code> 进行编译。</p><h3 id="cli命令行工具-babel-cli" tabindex="-1">CLI命令行工具 @babel/cli <a class="header-anchor" href="#cli命令行工具-babel-cli" aria-label="Permalink to &quot;CLI命令行工具 @babel/cli&quot;">​</a></h3><p><code>babel</code> 提供的命令行工具，主要是提供 <code>babel</code> 这个命令，适合安装在项目里。</p><p><code>@babel/node</code> 提供了 <code>babel-node</code> 命令，但是 <code>@babel/node</code> 更适合全局安装，不适合安装在项目里。</p><pre><code>npm install --save-dev @babel/core @babel/cli
</code></pre><p>现在你就可以在项目中使用 <code>babel</code> 进行编译啦（如果不安装 <code>@babel/core</code>，会报错噢）</p><p>将命令配置在 <code>package.json</code> 文件的 <code>scripts</code> 字段中:</p><pre><code>//...&quot;scripts&quot;: {    &quot;compiler&quot;: &quot;babel src --out-dir lib --watch&quot;}
</code></pre><p>使用 <code>npm run compiler</code> 来执行编译，现在我们没有配置任何插件，编译前后的代码是完全一样的。</p><p>因为 <code>Babel</code> 虽然开箱即用，但是什么动作也不做，如果想要 <code>Babel</code> 做一些实际的工作，就需要为其添加插件(<code>plugin</code>)。</p><h3 id="插件" tabindex="-1">插件 <a class="header-anchor" href="#插件" aria-label="Permalink to &quot;插件&quot;">​</a></h3><p><code>Babel</code> 构建在插件之上，使用现有的或者自己编写的插件可以组成一个转换通道，<code>Babel</code> 的插件分为两种: 语法插件和转换插件。</p><blockquote><p>语法插件</p></blockquote><p>这些插件只允许 <code>Babel</code> <strong>解析（parse）</strong> 特定类型的语法（不是转换），可以在 <code>AST</code> 转换时使用，以支持解析新语法，例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> babel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;@babel/core&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> code</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> babel.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">transformFromAstSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ast, {    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  //支持可选链    </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@babel/plugin-proposal-optional-chaining&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  babelrc: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .code;</span></span></code></pre></div><blockquote><p>转换插件</p></blockquote><p>转换插件会启用相应的语法插件(因此不需要同时指定这两种插件)，这点很容易理解，如果不启用相应的语法插件，意味着无法解析，连解析都不能解析，又何谈转换呢？</p><h4 id="插件的使用" tabindex="-1">插件的使用 <a class="header-anchor" href="#插件的使用" aria-label="Permalink to &quot;插件的使用&quot;">​</a></h4><p>如果插件发布在 <code>npm</code> 上，可以直接填写插件的名称， <code>Babel</code> 会自动检查它是否已经被安装在 <code>node_modules</code> 目录下，在项目目录下新建 <code>.babelrc</code> 文件 (下文会具体介绍配置文件)，配置如下：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    //.babelrc</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {    </span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@babelplugin-transform-arrow-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span></code></pre></div><p>也可以指定插件的相对/绝对路径</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {    </span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;./node_modules/@babel/plugin-transform-arrow-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span></code></pre></div><p>执行 <code>npm run compiler</code>，可以看到箭头函数已经被编译OK， <code>lib/index.js</code> 内容如下:</p><pre><code>const fn = function () {    console.log(&#39;a&#39;);};
</code></pre><p>现在，我们仅支持转换箭头函数，如果想将其它的新的JS特性转换成低版本，需要使用其它对应的 <code>plugin</code> 。如果我们一个个配置的话，会非常繁琐，因为你可能需要配置几十个插件，这显然非常不便，那么有没有什么办法可以简化这个配置呢？</p><p>有！预设！(感谢强大的 <code>Babel</code>)</p><h3 id="预设" tabindex="-1">预设 <a class="header-anchor" href="#预设" aria-label="Permalink to &quot;预设&quot;">​</a></h3><p>通过使用或创建一个 <code>preset</code> 即可<strong>轻松</strong>使用一组插件。</p><blockquote><p>官方 Preset</p></blockquote><ul><li>@babel/preset-env</li><li>@babel/preset-flow</li><li>@babel/preset-react</li><li>@babel/preset-typescript</li></ul><p><strong>注:</strong> 从 Babel v7 开始，所有针对标准提案阶段的功能所编写的预设(stage preset)都已被弃用，官方已经移除了 <code>@babel/preset-stage-x</code>。</p><h4 id="babel-preset-env" tabindex="-1">@babel/preset-env <a class="header-anchor" href="#babel-preset-env" aria-label="Permalink to &quot;@babel/preset-env&quot;">​</a></h4><p><code>@babel/preset-env</code> 主要作用是对我们所使用的并且目标浏览器中缺失的功能进行代码转换和加载 <code>polyfill</code>，在不进行任何配置的情况下，<code>@babel/preset-env</code> 所包含的插件将支持所有最新的JS特性(ES2015,ES2016等，不包含 stage 阶段)，将其转换成ES5代码。例如，如果你的代码中使用了可选链(目前，仍在 stage 阶段)，那么只配置 <code>@babel/preset-env</code>，转换时会抛出错误，需要另外安装相应的插件。</p><pre><code>//.babelrc{    &quot;presets&quot;: [&quot;@babel/preset-env&quot;]}
</code></pre><p>需要说明的是，<code>@babel/preset-env</code> 会根据你配置的目标环境，生成插件列表来编译。对于基于浏览器或 <code>Electron</code> 的项目，官方推荐使用 <code>.browserslistrc</code> 文件来指定目标环境。默认情况下，如果你没有在 <code>Babel</code> 配置文件中(如 .babelrc)设置 <code>targets</code> 或 <code>ignoreBrowserslistConfig</code>，<code>@babel/preset-env</code> 会使用 <code>browserslist</code> 配置源。</p><p>如果你不是要兼容所有的浏览器和环境，推荐你指定目标环境，这样你的编译代码能够保持最小。</p><p>例如，仅包括浏览器市场份额超过0.25％的用户所需的 <code>polyfill</code> 和代码转换（忽略没有安全更新的浏览器，如 IE10 和 BlackBerry）:</p><pre><code>//.browserslistrc&gt; 0.25%not dead
</code></pre><p>查看 <a href="https://github.com/browserslist/browserslist" target="_blank" rel="noreferrer"><code>browserslist</code> 的更多配置</a></p><p>例如，你将 <code>.browserslistrc</code> 的内容配置为:</p><pre><code>last 2 Chrome versions
</code></pre><p>然后再执行 <code>npm run compiler</code>，你会发现箭头函数不会被编译成ES5，因为 <code>chrome</code> 的最新2个版本都能够支持箭头函数。现在，我们将 <code>.browserslistrc</code> 仍然换成之前的配置。</p><p>就咱们目前的代码来说，当前的配置似乎已经是OK的了。</p><p>我们修改下 <code>src/index.js</code>。</p><pre><code>const isHas = [1,2,3].includes(2);const p = new Promise((resolve, reject) =&gt; {    resolve(100);});
</code></pre><p>编译出来的结果为:</p><pre><code>&quot;use strict&quot;;var isHas = [1, 2, 3].includes(2);var p = new Promise(function (resolve, reject) {  resolve(100);});
</code></pre><p>这个编译出来的代码在低版本浏览器中使用的话，显然是有问题的，因为低版本浏览器中数组实例上没有 <code>includes</code> 方法，也没有 <code>Promise</code> 构造函数。</p><p>这是为什么呢？因为语法转换只是将高版本的语法转换成低版本的，但是新的内置函数、实例方法无法转换。这时，就需要使用 <code>polyfill</code> 上场了，顾名思义，<code>polyfill</code>的中文意思是垫片，所谓垫片就是垫平不同浏览器或者不同环境下的差异，让新的内置函数、实例方法等在低版本浏览器中也可以使用。</p><h3 id="polyfill" tabindex="-1">Polyfill <a class="header-anchor" href="#polyfill" aria-label="Permalink to &quot;Polyfill&quot;">​</a></h3><p><code>@babel/polyfill</code> 模块包括 <code>core-js</code> 和一个自定义的 <code>regenerator runtime</code> 模块，可以模拟完整的 ES2015+ 环境（不包含第4阶段前的提议）。</p><p>这意味着可以使用诸如 <code>Promise</code> 和 <code>WeakMap</code> 之类的新的内置组件、 <code>Array.from</code> 或 <code>Object.assign</code> 之类的静态方法、<code>Array.prototype.includes</code> 之类的实例方法以及生成器函数(前提是使用了 <code>@babel/plugin-transform-regenerator</code> 插件)。为了添加这些功能，<code>polyfill</code> 将添加到全局范围和类似 <code>String</code> 这样的内置原型中(会对全局环境造成污染，后面我们会介绍不污染全局环境的方法)。</p><p>补充说明 (2020/01/07)：V7.4.0 版本开始，<code>@babel/polyfill</code> 已经被废弃(前端发展日新月异)，需单独安装 <code>core-js</code> 和 <code>regenerator-runtime</code> 模块。</p><p>首先，安装 <code>@babel/polyfill</code> 依赖:</p><pre><code>npm install --save @babel/polyfill
</code></pre><p>注意：不使用 <code>--save-dev</code>，因为这是一个需要在源码之前运行的垫片。</p><p>我们需要将完整的 <code>polyfill</code> 在代码之前加载，修改我们的 <code>src/index.js</code>:</p><pre><code>import &#39;@babel/polyfill&#39;;const isHas = [1,2,3].includes(2);const p = new Promise((resolve, reject) =&gt; {    resolve(100);});
</code></pre><p><code>@babel/polyfill</code> 需要在其它代码之前引入，我们也可以在 <code>webpack</code> 中进行配置。</p><p>例如:</p><pre><code>entry: [    require.resolve(&#39;./polyfills&#39;),    path.resolve(&#39;./index&#39;)]
</code></pre><p><code>polyfills.js</code> 文件内容如下:</p><pre><code>//当然，还可能有一些其它的 polyfill，例如 stage 4之前的一些 polyfillimport &#39;@babel/polyfill&#39;;
</code></pre><p>现在，我们的代码不管在低版本还是高版本浏览器(或node环境)中都能正常运行了。不过，很多时候，我们未必需要完整的 <code>@babel/polyfill</code>，这会导致我们最终构建出的包的体积增大，<code>@babel/polyfill</code>的包大小为89K (当前 <code>@babel/polyfill</code> 版本为 7.7.0)。</p><p>我们更期望的是，如果我使用了某个新特性，再引入对应的 <code>polyfill</code>，避免引入无用的代码。</p><p>值得庆幸的是， <code>Babel</code> 已经考虑到了这一点。</p><p><code>@babel/preset-env</code> 提供了一个 <code>useBuiltIns</code> 参数，设置值为 <code>usage</code> 时，就只会包含代码需要的 <code>polyfill</code> 。有一点需要注意：配置此参数的值为 <code>usage</code> ，必须要同时设置 <code>corejs</code> (如果不设置，会给出警告，默认使用的是&quot;corejs&quot;: 2) ，注意: 这里仍然需要安装 <code>@babel/polyfill</code>(当前 <code>@babel/polyfill</code> 版本默认会安装 &quot;corejs&quot;: 2):</p><p>首先说一下使用 <code>core-js@3</code> 的原因，<code>core-js@2</code> 分支中已经不会再添加新特性，新特性都会添加到 <code>core-js@3</code>。例如你使用了 <code>Array.prototype.flat()</code>，如果你使用的是 <code>core-js@2</code>，那么其不包含此新特性。为了可以使用更多的新特性，建议大家使用 <code>core-js@3</code>。</p><p>安装依赖依赖：</p><pre><code>npm install --save core-js@3
</code></pre><blockquote><p><a href="https://github.com/zloirock/core-js" target="_blank" rel="noreferrer">core-js (点击了解更多)</a> : JavaScript 的模块化标准库，包含 <code>Promise</code>、<code>Symbol</code>、<code>Iterator</code>和许多其他的特性，它可以让你仅加载必需的功能。</p></blockquote><p>现在，修改 <code>Babel</code> 的配置文件如下:</p><pre><code>//.babelrcconst presets = [    [        &quot;@babel/env&quot;,        {               &quot;useBuiltIns&quot;: &quot;usage&quot;,            &quot;corejs&quot;: 3        }    ]]
</code></pre><p><code>Babel</code> 会检查所有代码，以便查找在目标环境中缺失的功能，然后仅仅把需要的 <code>polyfill</code> 包含进来。</p><p>例如，<code>src/index.js</code> 代码不变：</p><pre><code>const isHas = [1,2,3].includes(2);const p = new Promise((resolve, reject) =&gt; {    resolve(100);});
</code></pre><p>我们看看编译出来的文件(<code>lib/index</code>):</p><pre><code>&quot;use strict&quot;;require(&quot;core-js/modules/es.array.includes&quot;);require(&quot;core-js/modules/es.object.to-string&quot;);require(&quot;core-js/modules/es.promise&quot;);var isHas = [1, 2, 3].includes(2);var p = new Promise(function (resolve, reject) {    resolve(100);});
</code></pre><p>同样的代码，我们用 <code>webpack</code> 构建一下(<code>production</code> 模式)，能看到最终的代码大小仅为: 20KB。而如果我们引入整个 <code>@babel/polyfill</code> 的话，构建出的包大小为：89KB</p><p>前面曾提到，在 <code>useBuiltIns</code> 参数值为 <code>usage</code> 时，仍然需要安装 <code>@babel/polyfill</code>，虽然我们上面的代码转换中看起来并没有使用到，但是，如果我们源码中使用到了 <code>async/await</code>，那么编译出来的代码需要 <code>require(&quot;regenerator-runtime/runtime&quot;)</code>，在 <code>@babel/polyfill</code> 的依赖中，当然啦，你也可以只安装 <code>regenerator-runtime/runtime</code> 取代安装 <code>@babel/polyfill</code>。</p><p>到了这一步，已经很棒棒了，是不是想跳起来转个圈圈？</p><p>下面我要说的内容，也许你已经知道，也许你还不知道，这都不重要，但是此刻起，你要知道了: <code>Babel</code> 会使用很小的辅助函数来实现类似 <code>_createClass</code> 等公共方法。默认情况下，它将被添加(<code>inject</code>)到需要它的每个文件中。</p><p>假如，我们的 <code>src/index.js</code> 是这样的:</p><pre><code>class Point {    constructor(x, y) {        this.x = x;        this.y = y;    };    getX() {        return this.x;    }}let cp = new ColorPoint(25, 8);
</code></pre><p>编译出来的 <code>lib/index.js</code>，如下所示:</p><pre><code>&quot;use strict&quot;;require(&quot;core-js/modules/es.object.define-property&quot;);function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(&quot;Cannot call a class as a function&quot;); } }function _defineProperties(target, props) { for (var i = 0; i &lt; props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (&quot;value&quot; in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }var Point =    /*#__PURE__*/    function () {        function Point(x, y) {            _classCallCheck(this, Point);            this.x = x;            this.y = y;        }        _createClass(Point, [{            key: &quot;getX&quot;,            value: function getX() {                return this.x;            }        }]);        return Point;    }();var cp = new ColorPoint(25, 8);
</code></pre><p>看起来，似乎并没有什么问题，但是你想一下，如果你有10个文件中都使用了这个 <code>class</code>，是不是意味着 <code>_classCallCheck</code>、<code>_defineProperties</code>、<code>_createClass</code> 这些方法被 <code>inject</code> 了10次。这显然会导致包体积增大，最关键的是，我们并不需要它 <code>inject</code> 多次。</p><p>这个时候，就是 <code>@babel/plugin-transform-runtime</code> 插件大显身手的时候了，使用 <code>@babel/plugin-transform-runtime</code> 插件，所有帮助程序都将引用模块 <code>@babel/runtime</code>，这样就可以避免编译后的代码中出现重复的帮助程序，有效减少包体积。</p><h3 id="babel-plugin-transform-runtime" tabindex="-1">@babel/plugin-transform-runtime <a class="header-anchor" href="#babel-plugin-transform-runtime" aria-label="Permalink to &quot;@babel/plugin-transform-runtime&quot;">​</a></h3><p><code>@babel/plugin-transform-runtime</code> 是一个可以重复使用 <code>Babel</code> 注入的帮助程序，以节省代码大小的插件。</p><blockquote><p>注意：诸如 <code>Array.prototype.flat()</code> 等实例方法将不起作用，因为这需要修改现有的内置函数(可以使用 <code>@babel/polyfill</code> 来解决这个问题) ——&gt; 对此需要说明的是如果你配置的是<code>corejs3</code>， <strong><code>core-js@3</code> 现在已经支持原型方法，同时不污染原型</strong>。</p></blockquote><p>另外，<code>@babel/plugin-transform-runtime</code> 需要和 <code>@babel/runtime</code> 配合使用。</p><p>首先安装依赖，<code>@babel/plugin-transform-runtime</code> 通常仅在开发时使用，但是运行时最终代码需要依赖 <code>@babel/runtime</code>，所以 <code>@babel/runtime</code> 必须要作为生产依赖被安装，如下 :</p><pre><code>npm install --save-dev @babel/plugin-transform-runtimenpm install --save @babel/runtime
</code></pre><p>除了前文所说的，<code>@babel/plugin-transform-runtime</code> 可以减少编译后代码的体积外，我们使用它还有一个好处，它可以为代码创建一个沙盒环境，如果使用 <code>@babel/polyfill</code> 及其提供的内置程序（例如 <code>Promise</code> ，<code>Set</code> 和 <code>Map</code> ），则它们将污染全局范围。虽然这对于应用程序或命令行工具可能是可以的，但是如果你的代码是要发布供他人使用的库，或者无法完全控制代码运行的环境，则将成为一个问题。</p><p><code>@babel/plugin-transform-runtime</code> 会将这些内置别名作为 <code>core-js</code> 的别名，因此您可以无缝使用它们，而无需 <code>polyfill</code>。</p><p>修改 <code>.babelrc</code> 的配置，如下:</p><pre><code>//.babelrc{    &quot;presets&quot;: [        [            &quot;@babel/preset-env&quot;,            {                &quot;useBuiltIns&quot;: &quot;usage&quot;,                &quot;corejs&quot;: 3            }        ]    ],    &quot;plugins&quot;: [        [            &quot;@babel/plugin-transform-runtime&quot;        ]    ]}
</code></pre><p>重新编译 <code>npm run compiler</code> , 现在，编译出来的内容为(<code>lib/index.js</code>):</p><pre><code>&quot;use strict&quot;;var _interopRequireDefault = require(&quot;@babel/runtime/helpers/interopRequireDefault&quot;);var _classCallCheck2 = _interopRequireDefault(require(&quot;@babel/runtime/helpers/classCallCheck&quot;));var _createClass2 = _interopRequireDefault(require(&quot;@babel/runtime/helpers/createClass&quot;));var Point =    /*#__PURE__*/    function () {        function Point(x, y) {            (0, _classCallCheck2.default)(this, Point);            this.x = x;            this.y = y;        }        (0, _createClass2.default)(Point, [{            key: &quot;getX&quot;,            value: function getX() {                return this.x;            }        }]);        return Point;    }();var cp = new ColorPoint(25, 8);
</code></pre><p>可以看出，帮助函数现在不是直接被 <code>inject</code> 到代码中，而是从 <code>@babel/runtime</code> 中引入。前文说了使用 <code>@babel/plugin-transform-runtime</code> 可以避免全局污染，我们来看看是如何避免污染的。</p><p>修改 <code>src/index.js</code> 如下：</p><pre><code>let isHas = [1,2,3].includes(2);new Promise((resolve, reject) =&gt; {    resolve(100);});
</code></pre><p>编译出来的代码如下(<code>lib/index.js</code>):</p><pre><code>&quot;use strict&quot;;require(&quot;core-js/modules/es.array.includes&quot;);require(&quot;core-js/modules/es.object.to-string&quot;);require(&quot;core-js/modules/es.promise&quot;);var isHas = [1, 2, 3].includes(2);new Promise(function (resolve, reject) {    resolve(100);});
</code></pre><p><code>Array.prototype</code> 上新增了 <code>includes</code> 方法，并且新增了全局的 <code>Promise</code> 方法，污染了全局环境，这跟不使用 <code>@babel/plugin-transform-runtime</code> 没有区别嘛。</p><p>如果我们希望 <code>@babel/plugin-transform-runtime</code> 不仅仅处理帮助函数，同时也能加载 <code>polyfill</code> 的话，我们需要给 <code>@babel/plugin-transform-runtime</code> 增加配置信息。</p><p>首先新增依赖 <code>@babel/runtime-corejs3</code>:</p><pre><code>npm install @babel/runtime-corejs3 --save
</code></pre><p>修改配置文件如下(移除了 <code>@babel/preset-env</code> 的 <code>useBuiltIns</code> 的配置，不然不就重复了嘛嘛嘛，不信的话，你用 <code>async/await</code> 编译下试试咯):</p><pre><code>{    &quot;presets&quot;: [        [            &quot;@babel/preset-env&quot;        ]    ],    &quot;plugins&quot;: [        [            &quot;@babel/plugin-transform-runtime&quot;,{                &quot;corejs&quot;: 3            }        ]    ]}
</code></pre><p>然后重新编译，看一下，编译出来的结果(<code>lib/index.js</code>):</p><pre><code>&quot;use strict&quot;;var _interopRequireDefault = require(&quot;@babel/runtime-corejs3/helpers/interopRequireDefault&quot;);var _promise = _interopRequireDefault(require(&quot;@babel/runtime-corejs3/core-js-stable/promise&quot;));var _includes = _interopRequireDefault(require(&quot;@babel/runtime-corejs3/core-js-stable/instance/includes&quot;));var _context;var isHas = (0, _includes.default)(_context = [1, 2, 3]).call(_context, 2);new _promise.default(function (resolve, reject) {  resolve(100);});
</code></pre><p>可以看出，没有直接去修改 <code>Array.prototype</code>，或者是新增 <code>Promise</code> 方法，避免了全局污染。如果上面 <code>@babel/plugin-transform-runtime</code> 配置的 <code>core-js</code> 是 &quot;2&quot;，其中不包含实例的 <code>polyfill</code> 需要单独引入。</p><blockquote><p>划重点：如果我们配置的 <code>corejs</code> 是 <code>3</code> 版本，那么不管是实例方法还是全局方法，都不会再污染全局环境。</p></blockquote><p>看到这里，不知道大家有没有这样一个疑问？给 <code>@babel/plugin-transform-runtime</code> 配置 <code>corejs</code> 是如此的完美，既可以将帮助函数变成引用的形式，又可以动态引入 <code>polyfill</code>，并且不会污染全局环境。何必要给 <code>@babel/preset-env</code> 提供 <code>useBuiltIns</code> 功能呢，看起来似乎不需要呀。</p><p>带着这样的疑问，我新建了几个文件(内容简单且基本一致，使用了些新特性)，然后使用 <code>webpack</code> 构建，以下是我对比的数据:</p><p>序号</p><p>.babelrc 配置</p><p>webpack mode production</p><p>0</p><p>不使用 <code>@babel/plugin-transform-runtime</code></p><p>36KB</p><p>1</p><p>使用<code>@babel/plugin-transform-runtime</code>，并配置参数 <code>corejs</code>: 3。不会污染全局环境</p><p>37KB</p><p>2</p><p>使用<code>@babel/plugin-transform-runtime</code>，不配置 <code>corejs</code></p><p>22KB</p><p>我猜测是 <code>@babel/runtime-corejs3/XXX</code> 的包本身比 <code>core-js/modules/XXX</code> 要大一些~</p><h3 id="插件-预设补充知识" tabindex="-1">插件/预设补充知识 <a class="header-anchor" href="#插件-预设补充知识" aria-label="Permalink to &quot;插件/预设补充知识&quot;">​</a></h3><blockquote><p><strong>插件的排列顺序很重要！！！</strong></p></blockquote><p>如果两个转换插件都将处理“程序（Program）”的某个代码片段，则将根据转换插件或 <code>preset</code> 的排列顺序依次执行。</p><ul><li>插件在 Presets 前运行。</li><li>插件顺序从前往后排列。</li><li>Preset 顺序是颠倒的（从后往前）。</li></ul><p>例如:</p><pre><code>{    &quot;plugins&quot;: [&quot;@babel/plugin-proposal-class-properties&quot;, &quot;@babel/plugin-syntax-dynamic-import&quot;]}
</code></pre><p>先执行 <code>@babel/plugin-proposal-class-properties</code>，后执行 <code>@babel/plugin-syntax-dynamic-import</code></p><pre><code>{  &quot;presets&quot;: [&quot;@babel/preset-env&quot;, &quot;@babel/preset-react&quot;]}
</code></pre><p><code>preset</code> 的执行顺序是<strong>颠倒</strong>的，先执行 <code>@babel/preset-react</code>， 后执行 <code>@babel/preset-env</code>。</p><h4 id="插件参数" tabindex="-1">插件参数 <a class="header-anchor" href="#插件参数" aria-label="Permalink to &quot;插件参数&quot;">​</a></h4><p>插件和 <code>preset</code> 都可以接受参数，参数由插件名和参数对象组成一个数组。<code>preset</code> 设置参数也是这种格式。</p><p>如:</p><pre><code>{    &quot;plugins&quot;: [        [            &quot;@babel/plugin-proposal-class-properties&quot;,             { &quot;loose&quot;: true }        ]    ]}
</code></pre><h5 id="插件的短名称" tabindex="-1">插件的短名称 <a class="header-anchor" href="#插件的短名称" aria-label="Permalink to &quot;插件的短名称&quot;">​</a></h5><p>如果插件名称为 <code>@babel/plugin-XXX</code>，可以使用短名称<code>@babel/XXX</code> :</p><pre><code>{    &quot;plugins&quot;: [        &quot;@babel/transform-arrow-functions&quot; //同 &quot;@babel/plugin-transform-arrow-functions&quot;    ]}
</code></pre><p>如果插件名称为 <code>babel-plugin-XXX</code>，可以使用短名称 <code>XXX</code>，该规则同样适用于带有 <code>scope</code> 的插件:</p><pre><code>{    &quot;plugins&quot;: [        &quot;newPlugin&quot;, //同 &quot;babel-plugin-newPlugin&quot;        &quot;@scp/myPlugin&quot; //同 &quot;@scp/babel-plugin-myPlugin&quot;    ]}
</code></pre><h4 id="创建-preset" tabindex="-1">创建 Preset <a class="header-anchor" href="#创建-preset" aria-label="Permalink to &quot;创建 Preset&quot;">​</a></h4><blockquote><p>可以简单的返回一个插件数组</p></blockquote><pre><code>module.exports = function() {    return {        plugins: [            &quot;A&quot;,            &quot;B&quot;,            &quot;C&quot;        ]    }}
</code></pre><blockquote><p><code>preset</code> 中也可以包含其他的 <code>preset</code>，以及带有参数的插件。</p></blockquote><pre><code>module.exports = function() {    return {        presets: [            require(&quot;@babel/preset-env&quot;)        ],        plugins: [            [require(&quot;@babel/plugin-proposal-class-properties&quot;), { loose: true }],            require(&quot;@babel/plugin-proposal-object-rest-spread&quot;)        ]    }}
</code></pre><h3 id="配置文件" tabindex="-1">配置文件 <a class="header-anchor" href="#配置文件" aria-label="Permalink to &quot;配置文件&quot;">​</a></h3><p>Babel 支持多种格式的配置文件。这部分内容补充了解下即可，谁管你用哪种配置文件，只要你的配置是OK的就可以了(敷衍)~</p><p>所有的 <code>Babel</code> API 参数都可以被配置，但是如果该参数需要使用的 JS 代码，那么可能需要使用 JS 代码版的配置文件。</p><blockquote><p>根据使用场景可以选择不同的配置文件:</p></blockquote><p>如果希望以编程的方式创建配置文件或者希望编译 <code>node_modules</code> 目录下的模块：那么 <code>babel.config.js</code> 可以满足你的需求。</p><p>如果只是需要一个简单的并且中用于单个软件包的配置：那么 <code>.babelrc</code> 即可满足你的需求。</p><h4 id="babel-config-js" tabindex="-1">babel.config.js <a class="header-anchor" href="#babel-config-js" aria-label="Permalink to &quot;babel.config.js&quot;">​</a></h4><p>在项目根目录下创建一个名为 <code>babel.config.js</code> 的文件。</p><pre><code>module.exports = function(api) {    api.cache(true);    const presets = [...];    const plugins = [...];    return {        presets,        plugins    };} 
</code></pre><p>具体的配置可以查看：<a href="https://www.babeljs.cn/docs/config-files#project-wide-configuration" target="_blank" rel="noreferrer">babel.config.js 文档</a></p><h4 id="babelrc" tabindex="-1">.babelrc <a class="header-anchor" href="#babelrc" aria-label="Permalink to &quot;.babelrc&quot;">​</a></h4><p>在项目根目录下创建一个名为 <code>.babelrc</code> 的文件：</p><pre><code>{    &quot;presets&quot;: [],    &quot;plugins&quot;: []}
</code></pre><p>具体的配置可以参考 <a href="https://www.babeljs.cn/docs/config-files#file-relative-configuration" target="_blank" rel="noreferrer">.babelrc 文档</a></p><h4 id="package-json" tabindex="-1">package.json <a class="header-anchor" href="#package-json" aria-label="Permalink to &quot;package.json&quot;">​</a></h4><p>可以将 <code>.babelrc</code> 中的配置信息作为 <code>babel</code> 键(key) 添加到 <code>package.json</code> 文件中:</p><pre><code>{    &quot;name&quot;: &quot;my-package&quot;,    &quot;babel&quot;: {        &quot;presets&quot;: [],        &quot;plugins&quot;: []    }}
</code></pre><h4 id="babelrc-js" tabindex="-1">.babelrc.js <a class="header-anchor" href="#babelrc-js" aria-label="Permalink to &quot;.babelrc.js&quot;">​</a></h4><p>与 <code>.babelrc</code> 配置相同，但是可以使用JS编写。</p><pre><code>//可以在其中调用 Node.js 的APIconst presets = [];const plugins = [];module.exports = { presets, plugins };
</code></pre><h2 id="原文" tabindex="-1">原文 <a class="header-anchor" href="#原文" aria-label="Permalink to &quot;原文&quot;">​</a></h2><p><a href="https://juejin.cn/post/6844904008679686152#heading-0" target="_blank" rel="noreferrer">https://juejin.cn/post/6844904008679686152#heading-0</a></p>`,187),a=[s];function l(i,p,d,n,u,b){return c(),o("div",null,a)}const g=e(t,[["render",l]]);export{q as __pageData,g as default};
